# Carregado por `next dev` e pelo serviço web-dev do compose (profile dev).
# API no host: com `make run-dev`, prefira a linha com host.docker.internal.
NEXT_PUBLIC_API_URL=http://localhost:3000
# NEXT_PUBLIC_API_URL=http://host.docker.internal:3000
