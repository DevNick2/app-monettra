# Referência de variáveis. Desenvolvimento: use .env.development (versionado).
# Produção (Docker/build): copie .env.production.example para .env.production (não commitado).

# URL base da API FastAPI.
NEXT_PUBLIC_API_URL=http://localhost:3000

# OAuth Google (login).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
